<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.tju.core.security.mappers.PersonMapper">

    <insert id="savePerson">
        insert into person values (#{email},#{firstName},#{gender},#{lastName},#{phone},#{photo},#{id})
    </insert>
</mapper>